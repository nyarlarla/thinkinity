<div class="menu">
	<button type="button" class="btn  btn-block btn-info" data-toggle="collapse" data-target="#demo">
		click me
	</button>
	<div id="demo" class="collapse" style="width:100%;">
		<div class="collapse_form">
			<%= form_for idea_board do |f| %>
				<table style="width:100%" cellpadding="20">
					<tr>
						<td style="width:20%"></td><td style="width:70%"><%= f.submit '登録', class:"btn btn-reg" %></td>
					</tr>
		            	<td><%= f.label :ジャンル, class: "badge-secondary" %></td>
		            	<td><%= f.collection_select :genre_id, Genre.all, :id, :genre_symbol, :prompt => "ジャンルを選択してください", class: "select" %>
		            	</td>
					<tr>
						<td><%= f.label :tag_list, :タグ, class: "badge-secondary" %></td>
						<td><%= f.text_field :tag_list, value: idea_board.tag_list.join(','), class: "form-control" %></td>
					</tr>
					<tr>
				        <td><%= f.label :head, :タイトル, class: "badge-secondary" %></td>
				        <td><%= f.text_field :head, class: "form-control" %></td>
			      	</tr>
			      	<tr>
				        <td><%= f.label :内容, class: "badge-secondary" %></td>
				        <td><%= f.text_area :body, class: "form-control" %></td>
			      	</tr>
			     </table>
			<% end %>
		</div>
	</div>
</div>